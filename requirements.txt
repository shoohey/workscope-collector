# WorkScope Collector - Windows runtime dependencies
# Python 3.11 (Windows x64)

# Screen capture (high-performance, multi-monitor)
mss==9.0.1

# Window event hooks, process info, foreground window
# pywin32==306 was yanked from PyPI; 307+ are the only available wheels
pywin32>=307,<312
psutil==5.9.8

# Image processing
Pillow==10.2.0
numpy==1.26.4
opencv-python-headless==4.9.0.80

# OCR (Japanese, fully offline once models are downloaded)
# paddlepaddle 2.6.0 was yanked; 2.6.2+ are the only available wheels
# Stay within 2.6.x range; paddleocr 2.7.3 is compatible with paddle 2.6.x
paddlepaddle>=2.6.2,<2.7
paddleocr==2.7.3

# Tray icon UI
pystray==0.19.5

# Config / logging
pydantic==2.6.1
python-dateutil==2.8.2

# Build
pyinstaller==6.4.0
